<p><%= t('devise.confirmations.welcome') %> <%= @resource.email %>!</p>

<p><%= t('devise.confirmations.confirm_below') %>:</p>
<p>:</p>

<p><%= link_to t('devise.confirm_account'), confirmation_url(@resource, :confirmation_token => @resource.confirmation_token) %></p>
