<%
content_for :title, t('archive.archive_month', month: l(@month, format: '%B %Y'), forum: current_forum ? current_forum.name : t('archive.all_forums'))
content_for :h1, t('archive.archive_month', month: l(@month, format: '%B %Y'), forum: current_forum ? current_forum.name : t('archive.all_forums'))
content_for :body_id, 'archive-month'
content_for :body_classes, 'archive month forum-' + (current_forum.try(:slug) || 'all')
%>

<div class="root" data-js="threadlist">
  <% for thread in @threads %>
    <%= thread_html(thread) %>
  <% end %>
</div>

<%= paginate @threads %>

<img src="//www.browser-statistik.de/browser.png?style=0"
     width="1" height="1" style="border: 0px;" alt="">
