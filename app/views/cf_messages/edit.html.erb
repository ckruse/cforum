<%
content_for :title, t('messages.edit_posting')
content_for :h1, t('messages.edit_posting')
content_for :body_id, 'messages-edit'
content_for :body_classes, 'messages edit forum-' + (current_forum.try(:slug) || 'all') + (@preview ? ' preview' : '')
%>

<article class="thread-message">

  <% if not @preview %>
    <%= message_header(@thread, @message.parent) %>

    <div class="posting-content">
      <%= @message.parent.to_html(self) %>
    </div>

  <% elsif @preview %>
    <%= message_header(@thread, @message) %>

    <div class="posting-content">
      <%= @message.to_html(self) %>
    </div>
  <% end %>
</article>

<%= form_for(@message, url: cf_message_path_wo_anchor(@thread, @message),
             html: {class: 'form-horizontal'}) do |f| %>
  <% if @message.errors.any? %>
    <div id="error_explanation" class="cf-error">
      <h4><%= t_numerus(@message.errors.count, 'messages.error_message_s', 'messages.error_message_p', num: @message.errors.count) %></h4>

      <ul>
        <% @message.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= render "form", f: f, with_name: true %>

  <ul id="charta">
    <li>Formuliere <a href="http://wiki.selfhtml.org/wiki/SELFHTML:Forum/Charta_des_SELFHTML-Forums"><strong>höflich und wertschätzend</strong></a>.</li>
    <li><strong>Vermeide</strong> <a href="http://de.wikipedia.org/wiki/Fullquote">Vollzitate</a>.</li>
    <li>Dein Beitrag wird <strong>dauerhaft</strong> <a href="http://forum.selfhtml.org/self/archive">archiviert</a>.</li>
  </ul>

  <p>
    <%= f.submit t('messages.preview'), :class => 'cf-btn', name: 'preview' %>
    <%= f.submit t('global.save'), :class => 'cf-primary-btn' %>
    <%= link_to t('global.cancel'), cf_message_path(@thread, @message), class: 'cf-btn' %>
  </p>
<% end %>

