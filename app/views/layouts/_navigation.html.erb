          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>

          <a class="brand" href="https://github.com/ckruse/cforum/">Classic Forum</a>

          <div class="nav-collapse">
            <ul class="nav">
              <li<% if controller_path == 'cf_forums' %> class="active"<% end %>><a href="<%= root_url %>"><%= t('views.home') %></a></li>
              <% if current_user and current_user.admin %>
              <li<% if controller_path =~ /^admin/ %> class="active"<% end %>><%= link_to t('views.administration'), admin_users_path %></li>
              <% end %>

              <% if current_forum %>
              <li<% if controller_path == 'cf_threads' or controller_path == 'cf_messages' %> class="active"<% end %>><%= link_to current_forum.name, current_forum %></li>
              <li><%= link_to t("views.new_thread"), new_cf_thread_path %></li><% end %>
            </ul>
            <ul class="nav pull-right">
              <% if user_signed_in? %>
                <li<% if controller_path == 'users' %> class="active"<% end %>><%= link_to(current_user.username, user_path(current_user)) %></li>
                <li><%= link_to(t('views.logout'), destroy_user_session_path, :method => :delete) %></li>
              <% else %>
                <li><%= link_to(t('views.login'), new_user_session_path) %></li>
                <li><%= link_to t("views.register"), new_user_registration_path %></li>
              <% end %>
            </ul>
          </div><!--/.nav-collapse -->
