<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= t ".title" %></title>

    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

    <script>
    <% if current_user %>current_user = <%== current_user.to_json(:except => [:crypted_password, :salt]) %>;<% end %>
    </script>

    <%= yield(:head) %>
  </head>

  <body data-controller="<%= controller_path %>" data-action="<%= action_name %>"<%= " id='#{yield(:body_id)}'" if content_for? :body_id %>>
    <header>
      <p class="welcome-message"><%= t ".welcome_message" %></p>
      <h1><%= link_to t(".forum_name"), root_path %></h1>
      <nav id="navigation">
      <ul>
        <li><%= link_to t(".new_thread"), new_cf_thread_path %></li>
        <li><%= link_to t(".register"), root_path %></li>
      </ul>
    </nav>
    </header>

    <div id="main">
      <div id="alerts-container">
      <% flash.each do |name,msg| %>
        <div class="alert alert-<%= name == :notice ? "success" : "error" %>">
          <%= msg %>
        </div>
      <% end %>
      </div>


      <%= yield %>
    </div>

  </body>
</html>
