<h1><%= t('views.user') + ' ' + @user.username %></h1>

<p>
  User has been created at <%= l(@user.created_at) %>. He created <%= @messages_count %> messages. User
  is <% unless @user.admin %><strong>not</strong><% end %> an admin. User is <% unless @user.active %><strong>not</strong><% end %>
  active.
</p>

<% unless @user.forums.blank? %><p>
  User is moderator in the followoing forums: <%= (@user.forums.map {|f| f.name }).join(", ") %>
</p><% end %>

<% if not current_user.blank? and @user.user_id == current_user.user_id %>
<form class="form-horizontal">
<%= render 'settings', ro: true %>
</form>
<% end %>

<p class="form-actions">
  <% if not current_user.blank? and @user.user_id == current_user.user_id %>
  <%= link_to t('views.edit'), edit_user_path(@user), :class => 'btn' %>
  <%= link_to t('views.delete_account'), user_path(@user), confirm: 'Are you sure that you want to delete your account?', :method => :delete, :class => 'btn' %>
  <% end %>

  <%= link_to t('views.back'), users_path, :class => 'btn' %>
</p>
