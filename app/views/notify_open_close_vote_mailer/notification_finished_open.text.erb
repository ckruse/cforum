Hallo <%= @user.username %>,

Die Nachricht mit dem Betreff „<%= @message.subject %>“ unter dem URL
<<%= @url %>> von <%= @message.author %> wurde durch eine Abstimmung
entsperrt. Grund:

<% if @message.open_vote.reason == 'custom' %>
  <%= @message.open_vote.custom_reason %>
<% else %>
  <%= t('messages.close_vote.' + @message.open_vote.reason) %>
<% end %>

LG,
 CForum
