Hallo <%= @user.username %>,

Die Nachricht Unter dem URL <<%= @url %>> von <%= @message.author %>
wurde markiert, damit ein Moderator darüber schaut:

Grund: <%- if @message.flags['flagged'] == 'custom' %><%= @message.flags["custom_reason"] %><% else %><%= t('messages.close_vote.' + @message.flags['flagged']) %><% end %>
<% if @message.flags['flagged'] == 'duplicate' %>Duplikat-URL: <%= cf_forum_url(@message.forum) + '/' + @message.flags['flagged_dup_url'] %><% end %>

LG,
 CForum
