  <% if @cf_forum.errors.any? %>
    <div id="error_explanation">
      <h2><%= t('admin.forums.error_message', :errors_count => pluralize(@cf_forum.errors.count, t("views.error"), t('views.errors'))) %></h2>

      <ul>
        <% @cf_forum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


<div class="control-group">
  <%= f.label :name, t('admin.forums.fields.name'), :class => 'control-label' %>
  <div class="controls">
    <%= f.text_field :name %>
  </div>
</div>

<div class="control-group">
  <%= f.label :short_name, t('admin.forums.fields.short_name'), :class => 'control-label' %>
  <div class="controls">
    <%= f.text_field :short_name %>
  </div>
</div>

<div class="control-group">
  <%= f.label :slug, t('admin.forums.fields.slug'), :class => 'control-label' %>
  <div class="controls">
    <%= f.text_field :slug %>
  </div>
</div>

<div class="control-group">
  <%= f.label :public, t('admin.forums.fields.public'), :class => 'control-label' %>
  <div class="controls">
    <%= f.check_box :public %>
  </div>
</div>

<div class="control-group">
  <%= f.label :description, t('admin.forums.fields.description'), :class => 'control-label' %>
  <div class="controls">
    <%= f.text_area :description %>
  </div>
</div>
