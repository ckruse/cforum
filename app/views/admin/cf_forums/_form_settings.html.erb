<fieldset class="cf-tab-pane" id="settings">
  <legend><%= t('admin.forums.settings_nam') %></legend>
  <%= render 'admin/cf_settings/settings' %>
</fieldset>

<fieldset class="cf-tab-pane" id="settings-close-votes">
  <legend><%= t('admin.forums.settings_close_votes') %></legend>
  <%= render 'admin/cf_settings/settings_close_votes' %>
</fieldset>
