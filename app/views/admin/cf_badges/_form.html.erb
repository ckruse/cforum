  <% if @badge.errors.any? %>
    <div id="error_explanation">
      <h2><%= t('admin.badges.error_message') %></h2>

      <ul>
        <% @badge.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<div class="cf-cgroup">
  <%= f.label :name %>
  <div class="cntrls"><%= f.text_field :name %></div>
</div>

<div class="cf-cgroup">
  <%= f.label :slug %>
  <div class="cntrls"><%= f.text_field :slug %></div>
</div>

<div class="cf-cgroup">
  <%= f.label :score_needed %>
  <div class="cntrls"><%= f.number_field :score_needed %></div>
</div>

<div class="cf-cgroup">
  <%= f.label :badge_type %>

  <div class="cntrls">
    <%= f.select(:badge_type,
                 options_for_select([
                   [t('admin.badges.badge_medal_types.upvote'), 'upvote'],
                   [t('admin.badges.badge_medal_types.downvote'), 'downvote'],
                   [t('admin.badges.badge_medal_types.retag'), 'retag'],
                   [t('admin.badges.badge_medal_types.flag'), 'flag'],
                   [t('admin.badges.badge_medal_types.visit_close_reopen'), 'visit_close_reopen'],
                   [t('admin.badges.badge_medal_types.create_tag'), 'create_tag'],
                   [t('admin.badges.badge_medal_types.edit_question'), 'edit_question'],
                   [t('admin.badges.badge_medal_types.edit_answer'), 'edit_answer'],
                   [t('admin.badges.badge_medal_types.create_tag_synonym'), 'create_tag_synonym'],
                   [t('admin.badges.badge_medal_types.create_close_reopen_vote'), 'create_close_reopen_vote'],
                   [t('admin.badges.badge_medal_types.moderator_tools'), 'moderator_tools'],
                   [t('admin.badges.badge_medal_types.custom'), 'custom']
                 ], @badge.badge_medal_type)) %>
  </div>
</div>


<div class="cf-cgroup">
  <%= f.label :badge_medal_type %>

  <div class="cntrls">
    <%= f.select(:badge_medal_type,
                 options_for_select([
                   [t('admin.badges.badge_medal_types.bronze'), 'bronze'],
                   [t('admin.badges.badge_medal_types.silver'), 'silver'],
                   [t('admin.badges.badge_medal_types.gold'), 'gold']
                 ], @badge.badge_medal_type)) %>
  </div>
</div>
