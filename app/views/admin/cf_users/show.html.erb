<h1>Benutzer <%= @user.username %></h1>

<p>
  E-Mail: <%= @user.email %><br>
  Postings: <%= @postings_count %><br>
  Aktiv: <%= @user.active ? 'Yes' : 'No' %><br>
  Admin: <%= @user.admin ? 'Yes' : 'No' %><br>
  Erstellt: <%= l(@user.created_at) %><br>
  Geändert: <%= l(@user.updated_at) %>
</p>

<p class="form-actions">
  <%= link_to 'Edit', edit_admin_user_path(@user), :class => 'btn' %>
  <%= link_to 'Back', admin_users_path(@user), :class => 'btn' %>
</p>
