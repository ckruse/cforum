<%
content_for :title, t('admin.users.new_user')
content_for :h1, "<h1>".html_safe + t('admin.users.new_user') + "</h1>".html_safe
%>

<%= form_for @user, url: admin_users_path, html: {class: 'form-horizontal'} do |f| %>

<%= render 'form', f: f %>

<% end %>
