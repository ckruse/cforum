<%
content_for :title, t('admin.users.edit_user', username: @user.username)
content_for :h1, "<h1>".html_safe + t('admin.users.edit_user', username: @user.username) + "</h1>".html_safe
%>

<%= form_for @user, url: admin_user_path(@user), html: {class: 'form-horizontal'} do |f| %>
  <%= render 'form', f: f %>
<% end %>
