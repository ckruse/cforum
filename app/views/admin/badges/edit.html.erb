<%
content_for :title, t('admin.badges.edit', name: @badge.name)
content_for :h1, t('admin.badges.edit', name: @badge.name)
%>

<%= form_for @badge, url: admin_badge_path(@badge), html: {class: 'cf-form'} do |f| %>

  <%= render 'form', f: f %>

  <p class="form-actions">
    <%= f.submit t('global.save'), class: 'cf-primary-btn' %>
    <%= cf_link_to t('global.cancel'), admin_badges_path, class: 'cf-btn' %>
  </p>

<% end %>
